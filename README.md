# Stock Price Prediction (Short-Term)

This project predicts the **next day’s closing price** of a stock using  
historical data from Yahoo Finance (`yfinance`).

## Objective
- Load stock data from Yahoo Finance  
- Use features like Open, High, Low, Volume  
- Train a **Linear Regression** or **Random Forest** model  
- Predict the **next closing price**  
- Plot **Actual vs Predicted** prices  

##  How to Run
1. Install dependencies:
